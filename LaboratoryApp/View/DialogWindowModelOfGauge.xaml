<UserControl x:Class="LaboratoryApp.View.DialogWindowModelOfGauge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:LaboratoryApp.ViewModel"
             xmlns:local="clr-namespace:LaboratoryApp"
             
             mc:Ignorable="d" Height="538" Width="515">
    <UserControl.Resources>
        <ControlTemplate x:Key="errorTemplate">
            <DockPanel LastChildFill="true">
                <Border Background="OrangeRed" DockPanel.Dock="right"
                    Margin="3,0,0,0" Width="20" Height="20" CornerRadius="5"
                    ToolTip="{Binding ElementName=adoner,
                        Path=AdornedElement.(Validation.Errors)[0].ErrorContent}">
                    <TextBlock Text="!" VerticalAlignment="center" HorizontalAlignment="center"
                           FontWeight="Bold" Foreground="white" />
                </Border>
                <AdornedElementPlaceholder Name="adoner" VerticalAlignment="Center">
                    <Border BorderBrush="OrangeRed" BorderThickness="1" />
                </AdornedElementPlaceholder>
            </DockPanel>
        </ControlTemplate>
        <Style x:Key="textBoxError" TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="true">
                    <Setter Property="ToolTip"
                        Value="{Binding RelativeSource={x:Static RelativeSource.Self},
                        Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ComboBoxError" TargetType="ComboBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="true">
                    <Setter Property="ToolTip"
                        Value="{Binding RelativeSource={x:Static RelativeSource.Self},
                        Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="210"/>
            <ColumnDefinition Width="*"/>


        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition  Height="Auto"/>
            <RowDefinition  Height="Auto"/>
            <RowDefinition  Height="Auto"/>
            <RowDefinition  Height="Auto"/>
            <RowDefinition  Height="Auto" MinHeight="73"/>
            <RowDefinition  Height="Auto" MinHeight="127"/>
            <RowDefinition  Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Producent: " Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" Height="26" Margin="0,0,0,23" Width="70"/>
        <Label Content="Model: " HorizontalAlignment="Left" Grid.Row="1" VerticalAlignment="Center" Height="26" Margin="0,20,0,1" Width="50"/>
        <Label Content="Zastosowanie: " HorizontalAlignment="Left" Grid.Row="2" VerticalAlignment="Center" Height="26" Margin="0,3,0,19" Width="88"/>
        <Label Content="Typ: " HorizontalAlignment="Left" Grid.Row="3" VerticalAlignment="Center" Height="26" Margin="0,19,0,3" Width="35"  />

        <TextBox Name="TxtBoxManufacturer" HorizontalAlignment="Center" VerticalAlignment="Center"
                 Grid.Row="0" Grid.Column="1" Width="210" Height="24" Grid.ColumnSpan="1"
                 Validation.ErrorTemplate="{StaticResource errorTemplate}"
                 Style="{StaticResource textBoxError}" Margin="1,2,0,23">
            <TextBox.Text>
                <Binding Path="AboutModelOfGauge.ManufacturerName" UpdateSourceTrigger="PropertyChanged" >
                    <Binding.ValidationRules>
                        <vm:ModelOfGaugesValidation ValidatesOnTargetUpdated="True"
                                     ValidationStep="RawProposedValue"
                                     ErrorMessage="Wpisz producenta [max. 50 znaków]." />
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
        <TextBox Name="TxtBoxModel" HorizontalAlignment="Center" VerticalAlignment="Center" 
                 Grid.Row="1" Grid.Column="1" Width="210" Height="24" Grid.ColumnSpan="1" 
                 Validation.ErrorTemplate="{StaticResource errorTemplate}"
                 Style="{StaticResource textBoxError}" Margin="1,20,0,3">
            <TextBox.Text>
                <Binding Path="AboutModelOfGauge.Model" UpdateSourceTrigger="PropertyChanged" >
                    <Binding.ValidationRules>
                        <vm:ModelOfGaugesValidation ValidatesOnTargetUpdated="True"
                                     ValidationStep="RawProposedValue"
                                     ErrorMessage="Wpisz model [max. 50 znaków]." />
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
        <!--<ListBox ItemsSource="{Binding AboutModelOfGauge.CollectionOfUsage}"  SelectedItem="{Binding SelectedUsage}" Grid.Row="2" Grid.Column="1"  Grid.RowSpan="1" Grid.ColumnSpan="1" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding description}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>-->
        <ComboBox Name="ComboBoxUsage" ItemsSource="{Binding AboutModelOfGauge.CollectionOfUsage}"  
                  Grid.Row="2" Grid.Column="1"  Grid.RowSpan="1" Grid.ColumnSpan="1" 
                  Validation.ErrorTemplate="{StaticResource errorTemplate}"
                  Style="{StaticResource ComboBoxError}">
            <ComboBox.SelectedItem>
                <Binding Path="AboutModelOfGauge.SelectedUsage" UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <vm:SelectedItemValidation ValidatesOnTargetUpdated="True"
                                     ValidationStep="RawProposedValue"
                                     ErrorMessage="Wybierz zastosowanie."/>
                    </Binding.ValidationRules>
                </Binding>
            </ComboBox.SelectedItem>
        </ComboBox>

        <ComboBox Name="ComboBoxType" ItemsSource="{Binding AboutModelOfGauge.CollectionOfType}"
                  VerticalAlignment="Center" Grid.Row="3" Grid.Column="1" Grid.RowSpan="1" 
                  Grid.ColumnSpan="1"  
                  Validation.ErrorTemplate="{StaticResource errorTemplate}"
                  Style="{StaticResource ComboBoxError}" Height="22">
            <ComboBox.SelectedItem>
                <Binding Path="AboutModelOfGauge.SelectedType" UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <vm:SelectedItemValidation ValidatesOnTargetUpdated="True"
                                     ValidationStep="RawProposedValue"
                                     ErrorMessage="Wybierz typ."/>
                    </Binding.ValidationRules>
                </Binding>
            </ComboBox.SelectedItem>
        </ComboBox>

        <vm:ModalDialogManager DataContext="{Binding MessageWindowType}" IsOpen="{Binding IsOpen}" ToConfirm="{Binding ToConfirm}"  DialogHeight="200" 
			DialogWidth="340" DialogResizeMode="CanResize" />

        <vm:ModalDialogManager DataContext="{Binding MessageWindowUsage}" IsOpen="{Binding IsOpen}" ToConfirm="{Binding ToConfirm}" DialogHeight="300" 
			DialogWidth="600" DialogResizeMode="CanResize"/>
        <vm:ModalDialogManager DataContext="{Binding MessageWindowCalibrator}" IsOpen="{Binding IsOpen}" ToConfirm="{Binding ToConfirm}"  DialogHeight="200" 
			DialogWidth="370" DialogResizeMode="CanResize" />
        <vm:ModalDialogManager DataContext="{Binding MessageWindowFunction}" IsOpen="{Binding IsOpen}" ToConfirm="{Binding ToConfirm}"  DialogHeight="200" 
			DialogWidth="370" DialogResizeMode="CanResize" />
        <vm:ModalDialogManager DataContext="{Binding MessageWindowTable}" IsOpen="{Binding IsOpen}" ToConfirm="{Binding ToConfirm}"  DialogHeight="600" 
			DialogWidth="370" DialogResizeMode="CanResize" />


        <Button Content="OK" Command="{Binding OKCommand}" HorizontalAlignment="Left" Grid.Row="7" VerticalAlignment="Center" Width="75" Grid.Column="1" Height="22" Margin="0,5,0,19">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="IsEnabled" Value="false" />
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <Setter Property="IsEnabled" Value="true" />
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=TxtBoxManufacturer,
                            Path=(Validation.HasError)}" Value="false" />
                                <Condition Binding="{Binding ElementName=TxtBoxModel,
                            Path=(Validation.HasError)}" Value="false"/>
                                <Condition Binding="{Binding ElementName=ComboBoxUsage,
                            Path=(Validation.HasError)}" Value="false"/>
                                <Condition Binding="{Binding ElementName=ComboBoxType,
                            Path=(Validation.HasError)}" Value="false"/>
                            </MultiDataTrigger.Conditions>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
        <Button Content="Cancel" Command="{Binding CancelCommand}" Grid.Column="1" HorizontalAlignment="Right" Grid.Row="7" VerticalAlignment="Center" Width="75" Height="26" Margin="0,3,0,17"/>
        <Button Content="Dodaj zastosowanie" Command="{Binding AddUsageCommand}" Grid.Column="2" HorizontalAlignment="Center" Grid.Row="2" VerticalAlignment="Center" Width="131" Height="25"/>
        <Button Content="Dodaj typ" Command="{Binding AddTypeCommand}" Grid.Column="2" HorizontalAlignment="Center"  Grid.Row="3" VerticalAlignment="Center" Width="131" Height="25"/>
        <Button Content="Dodaj Kalibrator" Command="{Binding AddCalibratorCommand}" Grid.Column="2" HorizontalAlignment="Center"  Grid.Row="6" VerticalAlignment="Center" Width="132" Height="26"/>
        <Button Content="Dodaj funkcje" Command="{Binding AddFunctionCommand}" Grid.Column="2" HorizontalAlignment="Center"  Grid.Row="5" VerticalAlignment="Center"  Width="132" Height="26"/>
        <Button Content="Dodaj tabelę" Command="{Binding AddTableCommand}" Grid.Column="2" HorizontalAlignment="Center"  Grid.Row="4" VerticalAlignment="Center" Width="132" Height="26"/>

        <ListBox Grid.Column="1" HorizontalAlignment="Left"  Grid.Row="6" VerticalAlignment="Top"  ItemsSource="{Binding CollectionOfCalibrators}" Width="211" Height="107" Margin="0,10,0,0" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsChecked}" Content="{Binding name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Label Content="Kalibratory: " HorizontalAlignment="Left" Grid.Row="6" VerticalAlignment="Center"  Margin="15,52,0,49" Height="26" Width="73"  />
        <Label Content="Sprawdzone funkcje: " HorizontalAlignment="Left" Grid.Row="5" VerticalAlignment="Center"  Margin="10,21,0,26" Height="26" Width="122"  />
        <ListBox Grid.Column="1" HorizontalAlignment="Left"  Grid.Row="5" VerticalAlignment="Top"  ItemsSource="{Binding CollectionOfCheckedFunction}" Width="211" Height="68" Grid.RowSpan="2" Grid.ColumnSpan="2" Margin="0,10,0,0" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsChecked}" Content="{Binding name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Label Content="Tabele: " HorizontalAlignment="Left" Grid.Row="4" VerticalAlignment="Center"  Margin="10,21,0,26" Height="26" Width="122"  />
        <ListBox Grid.Column="1" HorizontalAlignment="Left"  Grid.Row="4" VerticalAlignment="Top"  ItemsSource="{Binding ListOfNamesOfTables}" Width="211" Height="69" Grid.ColumnSpan="2" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsChecked}" Content="{Binding Name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>



    </Grid>
</UserControl>
