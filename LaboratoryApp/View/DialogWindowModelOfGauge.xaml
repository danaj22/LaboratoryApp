<UserControl x:Class="LaboratoryApp.View.DialogWindowModelOfGauge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:LaboratoryApp.ViewModel"
             xmlns:local="clr-namespace:LaboratoryApp"
             
             mc:Ignorable="d"  
             d:DesignHeight="300" d:DesignWidth="300">
    <!--<d:DataContext>
        <vm:NewWindowModelOfGauge/>
    </d:DataContext>-->
    <Grid Margin="0,0,-153,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>


        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition  Height="Auto"/>
            <RowDefinition  Height="Auto"/>
            <RowDefinition  Height="Auto"/>
            <RowDefinition  Height="Auto"/>
            <RowDefinition  Height="Auto"/>
            <RowDefinition  Height="Auto"/>
            <RowDefinition  Height="Auto"/>
            <RowDefinition  Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Producent: " Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <Label Content="Model: " HorizontalAlignment="Left" Grid.Row="1" VerticalAlignment="Center"/>
        <Label Content="Zastosowanie: " HorizontalAlignment="Left" Grid.Row="2" VerticalAlignment="Center"/>
        <Label Content="Typ: " HorizontalAlignment="Left" Grid.Row="3" VerticalAlignment="Center"  />

        <TextBox HorizontalAlignment="Center" Text="{Binding AboutModelOfGauge.ManufacturerName}" VerticalAlignment="Center" Grid.Row="0" Grid.Column="1" Width="210" Height="24" Grid.ColumnSpan="2"/>
        <TextBox HorizontalAlignment="Center" Text="{Binding AboutModelOfGauge.Model}" VerticalAlignment="Center" Grid.Row="1" Grid.Column="1" Width="210" Height="24" Grid.ColumnSpan="2"/>
        <!--<ListBox ItemsSource="{Binding AboutModelOfGauge.CollectionOfUsage}"  SelectedItem="{Binding SelectedUsage}" Grid.Row="2" Grid.Column="1"  Grid.RowSpan="1" Grid.ColumnSpan="1" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding description}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>-->
        <ComboBox ItemsSource="{Binding AboutModelOfGauge.CollectionOfUsage}"  SelectedItem="{Binding AboutModelOfGauge.SelectedUsage}" Grid.Row="2" Grid.Column="1"  Grid.RowSpan="1" Grid.ColumnSpan="1" />

        <ComboBox HorizontalAlignment="Center" ItemsSource="{Binding AboutModelOfGauge.CollectionOfType}" SelectedItem="{Binding AboutModelOfGauge.SelectedType}" VerticalAlignment="Center" Grid.Row="3" Grid.Column="1" Width="210" Height="24"/>
            


        <Button Grid.ColumnSpan="2" Content="OK" Command="{Binding OKCommand}" HorizontalAlignment="Left" Margin="79,10,0,0" Grid.Row="7" VerticalAlignment="Top" Width="75"/>
        <Button Content="Cancel" Command="{Binding CancelCommand}" Grid.Column="1" HorizontalAlignment="Left" Margin="79,10,0,0" Grid.Row="7" VerticalAlignment="Top" Width="75"/>

    </Grid>
</UserControl>
