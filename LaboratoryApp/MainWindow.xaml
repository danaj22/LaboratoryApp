<Window x:Class="LaboratoryApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:LaboratoryApp"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <local:TreeViewHelper x:Key="data"/>
    </Window.Resources>
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition>
            </ColumnDefinition>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition>

            </RowDefinition>

            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Label Grid.Column="0" Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Content="Data:"/>
            <Label Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Temperatura:"/>
            <Label Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Bottom" Content="Imię i Nazwisko:"/>
        </StackPanel>


        <DatePicker  Grid.Column="1" Margin="0,10,0,73" FirstDayOfWeek="Monday"/>
        <TextBox Grid.Column="1" Text="{Binding Temperature}" HorizontalAlignment="Left" Height="23" Margin="0,46,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
        <TextBox Grid.Column="1" Text="{Binding NameAndSurname}" HorizontalAlignment="Left" Height="23" Margin="0,74,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>

        <TreeView Grid.Row="1" ItemsSource="{Binding Data.TreeOfClients}" Grid.RowSpan="2">

            <TreeView.ItemTemplate>
                
                <HierarchicalDataTemplate ItemsSource="{Binding Offices}">
                    <TreeViewItem Header="{Binding Name}" />
                    <!--<TextBlock Text="{Binding Name}" />-->

                    <!--offices template-->
                    <HierarchicalDataTemplate.ItemTemplate>
                        <HierarchicalDataTemplate  ItemsSource="{Binding CollectionOfGauges}">
                            <TreeViewItem Header="{Binding Name}"/>
                            <!--<TextBlock Text="{Binding Name}"/>-->

                            <!--gauges template-->
                            <HierarchicalDataTemplate.ItemTemplate>
                                <DataTemplate>
                                    <TreeViewItem Header="{Binding Name}" />
                                    <!--<TextBlock Text="{Binding Name}"/>-->
                                </DataTemplate>
                            </HierarchicalDataTemplate.ItemTemplate>

                        </HierarchicalDataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>

                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>

        </TreeView>
        <TreeView Grid.Column="2" Grid.Row="0" Grid.RowSpan="1" ItemsSource="{Binding Data.TreeOfClients,UpdateSourceTrigger=PropertyChanged}" 
                  local:TreeViewHelper.SelectedItem="{Binding SelectedNode, Mode=TwoWay}">
            
        </TreeView>

    </Grid>
</Window>
